# Repo Map

## Confirmed from current repo snapshot

- router file exists and includes a live `/stones` route
- draw tool is canvas-based with pointer events
- transliteration tool exists
- auth provider exists
- app shell and nav-based layout exist

## To complete during local repo scan
- exact nav component structure
- exact stones component file contents
- backend route/controller layout
- current data loading path for server APIs
