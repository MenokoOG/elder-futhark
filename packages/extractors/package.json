{
  "name": "@efa/extractors",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo extractors lint placeholder",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@efa/schemas": "workspace:*",
    "cheerio": "^1.0.0"
  }
}
